
{
  "entities": {
    "SharedProfile": {
      "title": "SharedProfile",
      "description": "A publicly accessible professional profile snapshot.",
      "type": "object",
      "properties": {
        "profileData": {
          "type": "object",
          "description": "The full user profile data."
        },
        "ownerId": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{uid}/profile/main": {
      "schema": { "$ref": "#/backend/entities/SharedProfile" },
      "description": "Private professional vault data. Path requested by user."
    },
    "/shared-profiles/{profileId}": {
      "schema": { "$ref": "#/backend/entities/SharedProfile" },
      "description": "Publicly shared profile snapshots."
    }
  }
}
